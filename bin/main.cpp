#include <iostream>
#include <regex>
#include <string>
#include <vector>
#include <SqlQuery.h>


int main() {
    SqlQuery sql("CREATE TABLE hui (\n"
                 "   asda fewfe,\n"
                 "   wef recce, \n"
//                 "   FOREIGN KEY (OrderID) REFERENCES Orders(OrderID)\n"
                 ")\n"
                 "");

    return 0;
}
